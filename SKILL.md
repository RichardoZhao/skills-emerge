---
name: skills-emerge
version: 1.0.0
description: "Use when 技能库去重/合并/清理冗余。找冗余删除、找不同合并、相似整合成更全面的 skill。"
author: 小薇 / Hermes Agent
license: CC-BY-4.0 (文档) + MIT (示例代码)
metadata:
  hermes:
    tags: [skill管理, 去重, 合并, 冗余清理, skill库整理]
---

# Skills-Emerge — 技能库融合（去重 / 合并 / 整合）

> **⚠️ 先读这一句：本流程会删除文件。** 它在合并后会删掉被吸干的 skill。
> 动手前请自行备份；本流程不负责恢复。详见 README 顶部警示与「免责」一节。

## When to Use

你的技能库已经装到几十个，开始出现"这个好像跟那个是一回事"的情况；
或者你发现同一个功能有两三个版本，不知道留哪个。

目标：**让技能库在增长中保持整洁——同名删除、近义合并、相似整合，不丢信息。**

## 适用范围与边界

- **本 skill 出判据和清单，不自动合并。** "怎么合并"可以标准化（拆解框架、重叠分类、检查清单）；
  "这一步具体保谁、范围怎么对齐"必须具体 skill 具体判断。
- **不是所有重复都该合**：同领域但各管一段的（诊断 vs 配置、通用 vs 国内镜像）保持独立更清晰。
- **只有流水线型 skill 才查"接续"**（上一环产出 → 下一环输入）；纯规范/工具用法类不必套接口契约表，
  会变成壳大于核。
- **别引入分层架构**：skill 是 markdown 指令，不是代码模块；
  硬写「主流程 + 可插拔子模块」往往层级比内容还多。

## 核心理念

> "找到冗余的，删除；找到不同的，合并；将相似 skill 合并成同一个，更加完善，更加全面。"
> —— 赵老师，2026-09-09

由此推出三条：

- 你会持续添加 skill → 重复/重叠必然出现 → **定期融合是库的日常维护，不是一次性清理**
- 融合的对象是"知识与判据"，不是"文件整齐"——**只为整齐就不值当**
- 执行前提：**先展示方案与待删清单，批准后才动手**（删除纪律不因批量而豁免）

## 什么时候不该做这件事

**冷却期判据**：过去 30 天内被修改过的 skill 不进本轮合并。

> 「30 天」是个**自定阈值**，不是实测结论 —— 按你自己的修改频率调整。改动频繁的库可以缩到 7 天，稳定的库可以放宽。

理由：还在演化的东西被合并，等于把流动状态固化成壳，下一轮又要拆——**合并本身也会制造分叉**。

另外两种情况也先别合：

- 你还没想清楚它该负责哪一段（边界模糊时合并 = 把模糊固化）
- 只有一个 skill 涉及这个主题（没有重叠，谈不上融合）

## 流程（六步）

### 0. 准入判断（该不该合）

先过三关，不通过就停，别往下白做。**每一关都配一个可看的代理指标，不要凭感觉：**

| 关 | 问题 | 可看的代理量 | 不合格的样子 |
|---|---|---|---|
| ① | 触发场景是否相近？ | **触发语重合度**：两份 frontmatter/正文里的触发词有几个是同义的 | 一个管查询、一个管迁移 → 保持独立 |
| ② | 是否真实重叠？ | **是否被同一个任务链同时引用**：同时引用 = 该并；各自独立引用 = 别并 | 只是"都跟 X 有关"不算重叠 |
| ③ | 收益是否大于维护成本？ | **同一主题最近 N 次修改**：仍在高频演化的先别合（N 自定，一般取 3-5） | 只为整齐就不值当 |

**"明确不该合"的反例清单**（这几类遇到就停手）：

1. **各管一段的互补对**——诊断 vs 配置、通用 vs 国内镜像、查重 vs 清理。
   合成一个会让触发面变宽、误触发率上升。
2. **粒度不同的**——一个讲原则（10 行），一个讲操作（200 行带脚本）。
   合并会变成"壳大于核"。
3. **一个还在演化**——见上面的冷却期判据。
4. **同主题但受众不同**——给机器看的规范 vs 给人看的教程，混在一起两边都不好用。

### 1. 全库盘点（准确统计嵌套 skill）

⚠️ `find -maxdepth 2 -name SKILL.md` **会漏掉大多数 skill**——常见结构是
`分类/子skill/SKILL.md`（深度 3）。用递归统计：

```python
import os, collections
root = os.path.expanduser('~/.hermes/skills')      # 按你本机实际路径改
cat_count = collections.Counter(); cat_dirs = {}
for dp, dn, fn in os.walk(root):
    if '/.archive' in dp: continue
    if 'SKILL.md' in fn:
        parts = os.path.relpath(dp, root).split(os.sep)
        if len(parts) >= 2:
            cat_count[parts[0]] += 1
            cat_dirs.setdefault(parts[0], []).append(parts[-1])
print('总 skill:', sum(cat_count.values()), '| 分类:', len(cat_count))
for cat, n in sorted(cat_count.items(), key=lambda x: -x[1]):
    if n >= 5: print(f'### {cat} ({n})', ', '.join(sorted(cat_dirs[cat])))
```

**大分类是冗余高发区，优先扫。**

### 2. 自动探测疑似组（同名 + 近义）

- **同名重复**：按 name 分组，`len>1` 即疑似（不同分类可能同名）
- **近义组**：按业务主题手工列候选，再确认实际存在
- 探测脚本把每个候选 skill 的 `name/path/lines/desc/支持文件` 拉成底表——
  **判断依据不是描述，而是结构与文件**

### 3. 逐组精读对比（判断删/并/留）

对每个候选组：

- 提取章节标题大纲（`re.findall(r'^#{1,3} ', txt)`）对比结构
- 数 references/scripts/templates 支持文件（**有独有支持文件 = 可能含独有知识**）
- 检查脚本是否相同（md5）
- **近义 ≠ 冗余**：可能是互补 → 互补的保留并互设 `related_skills`，别硬合并

判定表：

| 类型 | 特征 | 处置 |
|---|---|---|
| A 纯重复 | 同一件事多个迭代版/中英文混杂版，内容高度重叠 | 保留最全的 1 个，其余删 |
| B 近义互补 | 主题相邻但各管一段（诊断/配置、通用/国内） | 都留，加 related_skills |
| C 超集包含 | 大版（几百行）含小版全部章节 | 留大版删小版 |
| D 需分层 | 通用流程 vs 案例 vs 工具混在一起 | 逐个读正文细判，列为下一批 |
| E **冲突** | 同一步骤给出**互相矛盾**的指令 | **不硬合**：改写成 if/else 分支，并留裁决记录 |

> **冲突 ≠ 重叠**：重叠是"重复"（去重选优即可），冲突是"矛盾"
> （硬合会让智能体在同一位置收到打架的指令，**比不合更糟**）。
> 合并前专门扫一遍"同一个动作两套说法"——术语、范围、授权口径不同都算。

### 4. 合并（先并后删，防丢信息）

保留项为壳，把待删项的独有段落（命令速查、实测记录、references 引用、分工说明）
用 targeted patch（old_string/new_string）补进壳。**不要全量重写**（会丢 references 链接）。

**合并前先做这一项检查**（不是事后复盘，是合并前的硬约束）：

> **两份的触发条件会不会打架？**
> 两份都写"当用户提到 X 时使用"，合并后触发面变宽、极易误触发
> → 先重写成一个**有边界的统一触发描述**，再合并。

### 5. 删除与验证

- **删除动作必须逐条单独执行**（不能批量），并且**删除前展示完整清单**，
  等明确确认再执行
- 删完做**存在性验证**：被删的找不到、保留的 SKILL.md 在
- **行为验证（这一步最容易糊弄自己）**：合并出错是"看不出来"的错，文件在不在说明不了问题。
  改成可判定的标准——

  > **合并后的 skill 必须能独立完成至少一个「原被删 skill」的完整任务**
  > （拿原 skill 的触发语去测）。做不到，就不许删原件。

- **回滚点与决策留痕**：动手前把待删原件留一份（`.archive/` 或同目录 `.bak_YYYYMMDD`）；
  合并完成后记一句决策——**为什么合、保留了谁、舍弃了什么**。
  合坏了能退回，日后也说得清。
- **同步克隆出来的副本**：如果工作区里有从主库克隆的独立副本（如按 profile 隔离的），
  它们会带着旧版冗余 skill。主库收敛后要同步处理。

## 坑（实测）

1. `find -maxdepth` 统计会漏深度 3 的 skill —— 用 `os.walk`
2. **同名 ≠ 重复**：不同目录的同名 skill 可能内容完全不同，先看再判
3. **中英文混杂的同一功能**往往是同一 skill 的翻译版 —— 纯重复
4. **删除操作不能批量**，逐个来；**补内容用 targeted replacement**
   （传全量内容会丢 references）
5. **删除纪律不豁免**：即使方案已批准，执行时仍展示清单确认
6. **新增 skill 前先查重**（尤其跨分类），从源头减少重复 ——
   融合是补救，防重才是根治
7. **粒度不统一会误判重叠**：A 拆 3 步、B 拆 10 步没法比。
   比对前先把两份都摊成同粒度（一个步骤＝一次不可再分的动作），再找重叠
8. **冲突别当重叠处理**：见判定表 E 类 ——
   同一动作两套矛盾说法必须分支化并留裁决记录，硬合出来的是"自相矛盾 skill"
9. **合并完成后要立刻验收触发边界**（坑 9 原本记作"坑"，实际上它是合并后马上会遇到的问题，
   已提到第 4 步的检查项）

## 实战案例

**一次真实的技能库收敛：从 395 个降到 379 个。**

> 口径说明：这是作者个人工作间的技能库规模，不含任何业务数据、客户信息或个人信息。

第一批（395 → 383）：

| 组 | 处置 |
|---|---|
| 浏览器数据导出类 5 个 | 留最全的 1 个，删 4 |
| 网页转 PDF 类 3 个 | 留最全的（含 3 个参考文件），删 2 |
| 技能盘点类 2 个 | 留主功能那个，删生成报表的 |
| office 自动化类 2 个 | 合并进主 skill，删旧的 |
| 字幕流水线类 4 个 | 留最全的，删 3 |
| 同类超集对 1 组（611 行 vs 152 行） | 留超集，删小版 |
| wsl 网络类 2 个 | **互补**，保留加互链 |
| python 环境类 2 个 | **互补**，保留 |

第二批（383 → 379）：

| 组 | 处置 |
|---|---|
| OCR 表格重建类 4 个 | 并入主 skill（吸收另一版独有的锚点/多值/字形/页码进阶），删 3；其余 3 个主题不同保留 |
| 同类工具 2 个 | 删重复的那个（主 skill 已覆盖） |
| 同类部署 2 个 | 删小版（另一个是超集） |
| 同类清理 2 个 | **互补**（清理纪律 vs 查重），保留 |
| 某一类文档相关 skill 4 个 | **待定**（等负责人决定后再融合，本轮未动） |

**这次收敛里最有价值的判断，是那两组"看起来该合、其实该留"的互补对**：
合并它们省不了多少加载开销，却会让触发面变宽、误触发率上升。

## 边界

- **不做自动合并**：本 skill 出判据和清单，最终取舍由人定
- **不处理"改内容"**：那是新 skill 创作，不是融合
- **不改别人的 skill 逻辑**：融合只做"合并同类项"，不重写功能

## Changelog

- **v1.0.0（对外版）** 从内部版脱敏重写：移除本地路径与内部称谓；
  补第 0 步的三个代理指标与"明确不该合"反例清单；
  补冷却期判据（30 天）；
  第 5 步的行为验证改为可判定标准；
  原坑 9 提到第 4 步作为合并前硬约束；
  案例表改为"组级描述"（不含具体 skill 名）。
