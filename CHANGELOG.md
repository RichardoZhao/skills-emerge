# Changelog

> 本项目的对外版本经过**三轮同行审阅**。以下记录每一轮按审阅意见落实的改动。
>
> 记录原则：只列**改动类别与落实结果**，不引用审阅意见原文
> （意见原文包含工作间的内部信息，引用等于把脱敏成果贴回去）。
> 人名与内部路径一概不出现。

---

## v1.0.0 — 首版（第 1 轮 + 第 2 轮 + 第 3 轮审阅后定稿）

### 第 3 轮落实的改动

| 类别 | 落实结果 |
|---|---|
| 变更未同步（同一处改了两处只改一处） | 许可理由在 `LICENSE-DOCS` 已换新版，README 对应段落仍是旧说法 → 两处统一，仓库内不再出现两种说法 |
| 记录先于事实发布 | 本文件先于 README 修正发布，会形成"公开记录与仓库自相矛盾" → 与 README 修正**同批**发布 |
| 规模数字缺口径 | 「395 → 379」补口径：作者个人工作间的收敛记录，非行业样本 |
| 脱敏回潮 | 平台文案里出现内部技能名（案例表已改组级描述，文案漏了）→ 改为组级描述 |
| 声明缺项 | 平台文案补「利益相关」声明 —— 与「AI 生成」声明是并列的两条硬要求 |

### 第 2 轮落实的改动

| 类别 | 落实结果 |
|---|---|
| 全称断言 | 「市面上所有去重方法都不识别矛盾」→ 降为「我见过的主流方法大多只处理重复、很少识别矛盾」——把"全面领先"降成"我的观察"，可信度不降反升 |
| 未实测的适用范围 | 「可直接用在任何 agent 框架」→ 改为结构性事实：「不依赖特定框架的加载机制，本质是对两个 markdown 文件做并/留判断」+ 明示「已在 Hermes 上实跑，其他框架请自行验证」 |
| 口径自相矛盾 | 一处说「一次真实的收敛」、另一处说「案例均为示例」→ 统一为「案例已脱敏；规模数字是作者工作间的真实记录，不含业务数据」 |
| 脱敏漏网 | 删掉「相关工具」整节（其中含两个内部技能名） |
| 许可链条断点 | 安装步骤补上两个 LICENSE 的复制命令 + README 补推荐署名格式；CC BY 的"保留署名"不再被复制动作架空 |
| 许可理由错误 | 原写「按 MIT 发布等于默许去掉署名」→ 这是硬错（MIT 明文要求保留版权与许可声明）。改为「MIT 是软件许可，管的是软件怎么被使用分发；本仓库主体是方法论文档，用软件许可去覆盖它，条款与对象不匹配」 |
| 占位符未回填 | 许可文件里的仓库地址占位符改成真实地址 |
| 可搜性（缺项） | 补上关键词组与英文摘要 —— 这三行决定「被搜到」这条路成不成立 |
| 差异化定位（缺项） | 补一句「它不提供技能，也不收录技能」——对抗同名领域的错位访客 |
| 自定阈值未标注 | 「过去 30 天」「最近 N 次」各加半句说明：这是自定阈值，不是实测结论，按自己的频率调整 |

### 第 1 轮落实的改动

| 类别 | 落实结果 |
|---|---|
| 过期人名 | 改为中性写法（「按 profile 隔离的副本」） |
| 硬编码路径 | 改为 `os.path.expanduser()` 写法，并注明按本机实际路径调整 |
| 判据太软 | 「该不该合」三关各配一个**可看的代理指标**（触发语重合度 / 是否被同一任务链同时引用 / 同主题最近修改次数），不再只给主观问句 |
| 缺反例清单 | 新增「明确不该合」四类反例（各管一段的互补对 / 粒度不同的 / 还在演化的 / 受众不同的） |
| 验证标准不可判定 | 行为验证改为可判定标准：「合并后的技能必须能独立完成至少一个原被删技能的完整任务，做不到就不许删原件」 |
| 检查项错位 | 把"合并后才会暴露的问题"从事后"坑"清单提到第 4 步，作为**合并前硬约束** |
| 缺适用边界 | 新增「什么时候不该做这件事」整节（含冷却期判据） |
| 情报泄漏 | 实战案例表全部改为**组级描述**（如「浏览器数据导出类 5 个」），不再出现具体技能名 |
| 安全性提示 | README 顶部独立警示「本流程会删文件」+ 免责第一条 |

---

## 关于本文件

本项目的审阅与改动记录按上述方式保留，用于两件事：

1. **让读者判断这套方法是否可信** —— 一份被反复挑过毛病的文档，比一份自说自话的文档更值得试。
2. **给愿意提意见的人一个示范** —— 意见怎么被落实、被拒绝、被自我更正的。
   （本版有一处改动来自审阅者**推翻自己上一轮的意见**，也如实记在上面。）
